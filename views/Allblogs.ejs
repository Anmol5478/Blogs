<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>All Blogs</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    body {
      background-color: #0f172a;
      color: white;
    }
  </style>
</head>
<body class="min-h-screen flex flex-col px-4 pb-10">

  <!-- ✅ Navbar -->
  <nav class="w-full bg-gray-900 py-4 px-6 flex justify-between items-center shadow-md">
    <h1 class="text-white text-2xl font-bold">MyBlog</h1>
    <div class="flex items-center gap-6">
      <a href="/" class="text-gray-300 hover:text-purple-400 transition">Home</a>
      <a href="/dashboard" class="text-gray-300 hover:text-purple-400 transition">Profile</a>
    </div>
  </nav>

  <!-- ✅ Main Content -->
  <div class="w-full max-w-2xl mx-auto mt-10">
    <h2 class="text-4xl font-bold text-center text-white mb-8">All Blogs</h2>

    <% blogs.forEach(blog => { %>
    <div class="bg-gray-800 rounded-2xl overflow-hidden shadow-lg mb-10 border border-gray-700">
      
      <!-- Thumbnail -->

      <!-- Content -->
      <div class="p-6">
        <p class="text-gray-300 leading-relaxed mb-4"><%= blog.content %></p>
        <h3 class="text-2xl font-bold text-purple-400 mb-2"><%= blog.title %></h3>
        <p class="text-gray-400 text-sm mb-4">Published on: <%= new Date(blog.createdAt).toLocaleDateString() %></p>

        <!-- Action -->
        <div class="flex justify-between items-center">
          <a href="/blogs/<%= blog._id %>" class="text-purple-500 hover:underline font-semibold">View Full Blog</a>
          <span class="text-sm text-gray-500 italic">#blogpost</span>
        </div>
      </div>
    </div>
    <% }) %>
  </div>

</body>
</html>
