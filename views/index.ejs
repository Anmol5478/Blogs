<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Your Blog</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        body {
            background-color: #000;
            overflow-x: hidden;
        }
        
        .bg-animation {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1;
        }
        
        .gradient-circle {
            position: absolute;
            border-radius: 50%;
            filter: blur(80px);
            opacity: 0.2;
        }
        
        .circle-1 {
            width: 300px;
            height: 300px;
            background: radial-gradient(circle, rgba(106, 13, 173, 0.8) 0%, rgba(0, 0, 0, 0) 70%);
            top: 20%;
            left: 10%;
            animation: float 8s ease-in-out infinite;
        }
        
        .circle-2 {
            width: 400px;
            height: 400px;
            background: radial-gradient(circle, rgba(56, 182, 255, 0.6) 0%, rgba(0, 0, 0, 0) 70%);
            bottom: 15%;
            right: 10%;
            animation: float 10s ease-in-out infinite;
        }
        
        .circle-3 {
            width: 250px;
            height: 250px;
            background: radial-gradient(circle, rgba(255, 94, 247, 0.5) 0%, rgba(0, 0, 0, 0) 70%);
            top: 50%;
            left: 50%;
            animation: float 12s ease-in-out infinite;
        }
        
        @keyframes float {
            0%, 100% {
                transform: translate(0, 0) rotate(0deg);
            }
            25% {
                transform: translate(10px, -15px) rotate(2deg);
            }
            50% {
                transform: translate(-10px, 10px) rotate(-2deg);
            }
            75% {
                transform: translate(15px, 5px) rotate(1deg);
            }
        }

        /* Enhanced 3D Window Styles */
        .device-mockup {
            perspective: 2000px;
            margin: 0 auto;
        }
        
        .laptop-container {
            position: relative;
            width: 100%;
            max-width: 800px;
            margin: 0 auto;
            animation: float-3d 8s ease-in-out infinite;
        }
        
        .laptop {
            position: relative;
            width: 100%;
            transform-style: preserve-3d;
            transform: rotateX(15deg) rotateY(-20deg);
            transition: transform 0.5s ease;
        }
        
        .laptop:hover {
            transform: rotateX(12deg) rotateY(-18deg);
        }
        
        .laptop-screen {
            position: relative;
            width: 100%;
            padding-bottom: 62.5%; /* 16:10 aspect ratio */
            background: #111;
            border: 12px solid #333;
            border-radius: 12px;
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.5);
            transform: translateZ(20px);
            overflow: hidden;
        }
        
        .laptop-screen img {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            object-fit: cover;
        }
        
        .laptop-base {
            position: absolute;
            width: 110%;
            height: 20px;
            background: #222;
            bottom: -15px;
            left: -5%;
            border-radius: 4px;
            transform: rotateX(80deg) translateZ(-10px);
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.7);
        }
        
        .laptop-shadow {
            position: absolute;
            width: 80%;
            height: 60px;
            background: radial-gradient(ellipse at center, rgba(56, 182, 255, 0.2) 0%, transparent 70%);
            bottom: -30px;
            left: 10%;
            transform: rotateX(90deg);
            filter: blur(15px);
            animation: shadow-pulse 8s ease-in-out infinite;
        }
        
        @keyframes float-3d {
            0%, 100% {
                transform: translateY(0) rotate(0deg);
            }
            25% {
                transform: translateY(-15px) rotate(1deg);
            }
            50% {
                transform: translateY(0) rotate(-1deg);
            }
            75% {
                transform: translateY(-10px) rotate(0.5deg);
            }
        }
        
        @keyframes shadow-pulse {
            0%, 100% {
                opacity: 0.6;
                width: 80%;
            }
            50% {
                opacity: 0.3;
                width: 90%;
            }
        }
    </style>
</head>
<body>
    <!-- Background Animation -->
    <div class="bg-animation">
        <div class="gradient-circle circle-1"></div>
        <div class="gradient-circle circle-2"></div>
        <div class="gradient-circle circle-3"></div>
    </div>

    <!-- Main Content -->
    <div class="min-h-screen flex flex-col items-center justify-center px-4 sm:px-6 lg:px-8 text-white">
        <div class="max-w-6xl w-full backdrop-blur-sm bg-black/30 p-8 rounded-xl border border-gray-800 shadow-2xl">
            <nav>
                <div class="flex justify-between items-center p-4 bg-gray-900/50 backdrop-blur-sm border-b border-gray-800">
                    <div class="text-lg font-bold text-white">My Blog</div>
                    <ul class="flex space-x-6">
                        <li><a href="/" class="text-gray-300 hover:text-white transition-colors">Home</a></li>
                        <li><a href="/about" class="text-gray-300 hover:text-white transition-colors">About</a></li>
                        <li><a href="/blog" class="text-gray-300 hover:text-white transition-colors">Blog</a></li>
                        <li><a href="/contact" class="text-gray-300 hover:text-white transition-colors">Contact</a></li>
                    </ul>
                    <div class="hidden md:flex space-x-4">
                        <a href="/login" class="text-gray-300 hover:text-white transition-colors">Login</a>
                        <a href="/register" class="text-gray-300 hover:text-white transition-colors">Sign Up</a>
                    </div>
            </nav>
            <header class="text-center mb-12">
                <h1 class="text-4xl sm:text-5xl md:text-6xl font-bold bg-clip-text text-transparent bg-gradient-to-r from-purple-400 to-blue-500 mb-4">
                    Welcome to My Blog
                </h1>
                <p class="text-lg sm:text-xl text-gray-300 max-w-2xl mx-auto">
                    Exploring ideas, sharing knowledge, and telling stories that matter.
                </p>
            </header>

            <main class="space-y-16">
                <!-- Enhanced 3D Blog Preview Section -->
                <section class="mb-16">
                    <div class="device-mockup">
                        <div class="laptop-container">
                            <div class="laptop">
                                <div class="laptop-screen">
                                    <!-- Replace with your actual blog screenshot -->
                                    <img src="https://images.unsplash.com/photo-1555066931-4365d14bab8c?ixlib=rb-1.2.1&auto=format&fit=crop&w=1350&q=80" alt="Blog Preview">
                                </div>
                                <div class="laptop-base"></div>
                                <div class="laptop-shadow"></div>
                            </div>
                        </div>
                    </div>
                </section>

                <!-- Featured Posts Section -->
                <section class="text-center">
                    <h2 class="text-2xl sm:text-3xl font-semibold mb-6">Featured Posts</h2>
                    <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                        <div class="bg-gray-900/50 p-6 rounded-lg border border-gray-800 hover:border-purple-500 transition-all duration-300">
                            <h3 class="text-xl font-medium mb-2">Post Title 1</h3>
                            <p class="text-gray-400 mb-4">A brief excerpt from the post that gives readers a preview...</p>
                            <a href="#" class="text-purple-400 hover:text-purple-300 font-medium">Read More →</a>
                        </div>
                        <div class="bg-gray-900/50 p-6 rounded-lg border border-gray-800 hover:border-blue-500 transition-all duration-300">
                            <h3 class="text-xl font-medium mb-2">Post Title 2</h3>
                            <p class="text-gray-400 mb-4">A brief excerpt from the post that gives readers a preview...</p>
                            <a href="#" class="text-blue-400 hover:text-blue-300 font-medium">Read More →</a>
                        </div>
                    </div>
                </section>

                <section class="text-center pt-8">
                    <a href="/blog" class="inline-block px-8 py-3 bg-gradient-to-r from-purple-600 to-blue-600 rounded-full font-semibold hover:from-purple-700 hover:to-blue-700 transition-all duration-300 shadow-lg hover:shadow-xl">
                        Explore All Posts
                    </a>
                </section>
            </main>

            <footer class="mt-16 text-center text-gray-500 text-sm">
                <p>© <%= new Date().getFullYear() %> Your Blog Name. All rights reserved.</p>
                <div class="flex justify-center space-x-4 mt-4">
                    <a href="#" class="hover:text-white transition-colors">Twitter</a>
                    <a href="#" class="hover:text-white transition-colors">GitHub</a>
                    <a href="#" class="hover:text-white transition-colors">LinkedIn</a>
                    <a href="#" class="hover:text-white transition-colors">RSS</a>
                </div>
            </footer>
        </div>
    </div>
</body>
</html>